// eslint-disable-next-line @typescript-eslint/no-unused-vars
import { h, Component, ComponentInterface } from '@stencil/core';

@Component({
  tag: 'cho-{{ name }}',
  styleUrl: '{{ name }}.scss',
  shadow: true,
})
export class {{ properCase name }} implements ComponentInterface {
  render() {
    return <div>{{ properCase name }}</div>;
  }
}
