import { RouteRenderProps } from '@stencil/router';
import { FunctionalComponent, h } from '@stencil/core';

export const NotFoundPage: (props: RouteRenderProps) => FunctionalComponent = () => <div>Not Found</div>;
