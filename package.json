{
  "name": "root",
  "private": true,
  "devDependencies": {
    "commitizen": "^4.1.2",
    "cz-conventional-changelog": "^3.2.0",
    "lerna": "^3.22.1",
    "standard-version": "^8.0.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "lerna --scope @cho/components run serve --stream",
    "release": "standard-version",
    "lerna": "lerna"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
